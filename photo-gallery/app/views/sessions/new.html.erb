<section class="sign-in">
  <h1>Sign in</h1>

  <% if flash[:alert] %><div class="flash flash-alert"><%= flash[:alert] %></div><% end %>
  <% if flash[:notice] %><div class="flash flash-notice"><%= flash[:notice] %></div><% end %>

  <%= form_with url: session_path do |form| %>
    <div class="field">
      <%= form.label :email_address, "Email" %>
      <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", value: params[:email_address] %>
    </div>

    <div class="field">
      <%= form.label :password, "Password" %>
      <%= form.password_field :password, required: true, autocomplete: "current-password", maxlength: 72 %>
    </div>

    <%= form.submit "Sign in", class: "primary-button" %>
  <% end %>
</section>

<section class="demo-accounts">
  <h2>Demo accounts</h2>
  <p class="demo-hint">Password for all accounts is <code>password</code>.</p>
  <ul class="demo-list">
    <% [
      [ "admin@example.com", "Admin" ],
      [ "alice@example.com", "Alice" ],
      [ "bob@example.com",   "Bob"   ],
      [ "carol@example.com", "Carol" ]
    ].each do |email, label| %>
      <li>
        <%= button_to session_path, params: { email_address: email, password: "password" }, class: "demo-button" do %>
          <strong><%= label %></strong>
          <span class="demo-email"><%= email %></span>
        <% end %>
      </li>
    <% end %>
  </ul>
</section>
